pre-push: # githook name
  commands: # list of commands
    tests: # command name
      run: go test -v # command for execution
pre-commit: # githook name
  commands: # list of commands
    tests: # command name
      run: go test -v # command for execution
